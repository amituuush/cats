import React, { Component, PropTypes } from 'react';

class Card extends Component {
  render() {
    return (
      <div>
        Cats and quotes
      </div>
    );
  }
}

Card.propTypes = {

};

export default Card;
